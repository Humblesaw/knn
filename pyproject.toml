[project]
name = "knn"
version = "0.1.0"
description = "Reinforcement learning for flight control."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flightgear-python>=2.0.3",
    "ruff>=0.15.5",
]
